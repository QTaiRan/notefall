node_modules
dist
.DS_Store
*.local
*.tsbuildinfo
_local/
# Salamander Grand Piano V3 OGGs (~77 MB). Prepared locally via
# `npm run fetch-salamander`, deployed to R2 in prod.
public/samples/
# Env overrides — `.env.example` is the documented template.
.env
.env.production
.env.local
.env.production.local
